# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment / secrets
.env
.env.*
!.env.example

# Local SQLite DBs (used only for offline migration checks)
*.db

# Caches / coverage
.coverage
coverage.xml
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ML artifacts: registry.json AND the model binaries are tracked. The active model is ~2 KB
# (logistic regression); tracking them keeps the CI-built image self-contained so the
# deployed API can serve live /model/predict. Retrain output is small and reproducible.

# Node / frontend (Phase 4)
node_modules/
dist/
.vite/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Claude Code local (machine-specific permissions)
.claude/settings.local.json

# Local working notes (not part of the public project)
HANDOFF.md
